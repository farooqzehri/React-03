import React from 'react'


function Products() {
    // const {error , loading , data} = useFetch('https://fakestoreapiserver.reactbd.org/api/products')
  return (
<>
<h1>Products</h1>
{/* {error && <h2>errro...</h2>}
{loading && <h1>loading...</h1>}
{data && data.map(item => {
    return <div key={item.title}>
        <h1>{item.title}</h1>
    </div>
})} */}
</>
  )
}

export default Products